{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Copy file",
            "command": "${workspaceFolder}/build/build.sh ${workspaceFolder}",
            "isBackground": true
        }
    ]
}
